import { NButton, NButtonGroup, NConfigProvider, NInput, NSpinner } from '@ui/components'
import type { Plugin } from 'vue'

export const Components: Plugin[] = [NButton, NButtonGroup, NConfigProvider, NSpinner, NInput]
